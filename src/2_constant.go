package main

import "fmt"

func main() {
	const LENGTH int = 10
	// A constant has to be declared with an initial value.
	const DECISION = false

	fmt.Println("Since the length is", LENGTH, ". The decision is", DECISION)
}
